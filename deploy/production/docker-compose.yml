services:
  app:
    image: template-sync-prod
    build:
      context: ../../
      dockerfile: ./deploy/production/dockerfile
    ports:
      - 80:34656
    env_file:
      - ../../.env/.env.production.web
    restart: always
    volumes:
      - ../../db:/opt/app/db
    logging:
      options:
        max-size: '100m'
        max-file: '5'
